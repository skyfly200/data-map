{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/mapChunks.mjs && node scripts/precompress_public.mjs",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "node --import ./tests/register.mjs --test \"tests/**/*.test.mjs\" && python -m pytest tests/ -q",
    "test:js": "node --import ./tests/register.mjs --test \"tests/**/*.test.mjs\"",
    "test:py": "python -m pytest tests/ -q"
  },
  "dependencies": {
    "@google/earthengine": "^1.7.43",
    "@netlify/blobs": "^8.1.0",
    "@supabase/supabase-js": "^2.116.0",
    "geotiff": "^2.1.3",
    "leaflet": "^1.9.4",
    "nuxt": "^3.16.2",
    "qrcode-generator": "^2.0.4",
    "vue": "^3.5.13"
  }
}
