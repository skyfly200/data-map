# Python data pipeline dependencies

# Observation ingest (iNat.py)
pyinaturalist
meteostat

# Environmental enrichment (ee_enrich.py) — Earth Engine supplies every
# environmental layer as a point sample, so this is the whole download stack.
earthengine-api
requests

# Enrichment / clustering maths
numpy
scipy
pandas
scikit-learn

# Raster fallback: only needed when Earth Engine is unavailable, or when
# FETCH_RASTERS=1 downloads the source rasters (also what validate_wetness.py
# raster mode and the Coverage page read).
rasterio
rio-cogeo
xarray
netCDF4
cdsapi
